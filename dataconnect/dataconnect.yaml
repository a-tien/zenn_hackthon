specVersion: "v1"
serviceId: "travel-plan"
location: "asia-northeast1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "travel-plan-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
